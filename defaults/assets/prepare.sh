#!/usr/bin/env bash

# This script is now deprecated and no longer used.
# The plugin now uses the extract_static_optiscaler method in main.py
# to extract the bundled OptiScaler archive directly.

echo "❌ This script is deprecated. The plugin now uses internal extraction."
echo "📄 Please use the plugin's Install button instead."
exit 1